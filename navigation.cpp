create a singleton class which maintains a 2 dimmentional integer array. Have functions that creates entities
inside the navigation map, move entities around, and prints out the array in a stylized manner. 
